# TASK-R099-Q002: official-night efficiency switch coverage-only repair

Status: **FROZEN_BEFORE_EVALUATION_PNL**  
Registered: 2026-09-16 JST  
Family / study: `cash_first_hour_extreme_night_efficiency_switch` / `R099-Q002`

Q001 is first audited from its PnL-free artifacts.  Before any Q002 PnL field is parsed, it must reproduce: scheduled axis 1,131; current-night unavailable 265; remaining 866 dates all below 100 complete nights in the exact prior 120 scheduled-date window; q-ready 0; and the R078-A/R079-A R097 hashes, E events, entry/exit timestamps and reasons, one-contract quantity, and opposite sides. Q001 must also show no daily-Net, PF, bootstrap, Delta, or sensitivity result artifact. Any mismatch is **INCONCLUSIVE before PnL**.

Only if that reproduction passes, Q002 reuses the immutable R078-A/R079-A data, costs, event/entry/exit rules, one-contract execution, and routes A/C/F/I/N exactly as in Q001. `e=abs(C-O)/(H-L)` and `v=H-L` remain the complete official NIGHT-through-05:29 measurements available by 08:45. The sole repair is state-history coverage: from exactly the previous 180 scheduled TSE business dates, excluding the current date, take the latest 100 complete nights; do not use a current night, a future night, or any observation outside that window, and do not replenish the fixed 100-observation sample from older dates. q33/q67 nearest-rank `e` determines LE/ME/HE; q33/q67 `v` provides the fixed mechanism bands.

Before PnL, audit complete-night count distribution and unavailable reasons by trade year and old/new schedule regime. Required gates are q-ready >=650; HE and LE >=180 each; complete A trades >=80; HE/R079 and LE/R078 >=25 each; long and short >=25 each; each of 2022--2024 >=15 and 2025H1 >=8; old/new >=70/8; each HE/LE by `v` band common-event cell >=5; and no future reference, same-day availability selection, R004 contamination, unexplained exclusion, or unresolved filled position. Failure is **INCONCLUSIVE before PnL**.

After that gate only, use the Q001 common 20-trade-date, non-wrapping, tail-truncated moving-block bootstrap (10,000 repetitions, seed `20260916`, linear percentile) on the common scheduled axis for A daily Net, paired A-C/A-F/A-I, and equal-`v`-band `Delta = mean(G|HE,v)-mean(G|LE,v)` averaged across the three bands, where `G=Net(R079)-Net(R078)`. Primary AND is A Net>0, PF>1, and positive lower CIs for A daily Net, all three paired differences, and Delta. Any failure is **REJECT**.

Only after a primary pass, fixed diagnostics are q25/q75, q40/q60, 80 and 120 complete nights within 180 scheduled dates, 100 complete nights within maximum 150 and 210 scheduled dates, and unchanged-route one-way 2/3 ticks and doubled fees. A sensitivity without all six HE/LE-by-`v` cells is insufficient, never PASS. To reach the hard ceiling of **INVESTIGATE**, every executable fixed sensitivity must have A Net>0 and Delta>0, both A legs must be positive, at least two of 2022--2024 and 2025H1 positive, both regimes positive, and A Net after removing its top 10 winners positive. No result can exceed INVESTIGATE. No result-dependent state/boundary/side/strategy/entry/exit change, Walk Forward, 2025H2 OOS, or Final Holdout is permitted.
