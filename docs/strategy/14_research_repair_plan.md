# 14. 研究手順の修復・移行計画

改訂ID: **RG-20260915-01** / 2026-09-15 JST  
現在の運用: **PAUSED_METHOD_REPAIR**  
実施済み範囲: 文書改訂・パッケージ整合検査のみ。実コード・価格データ・保存済み取引台帳の監査は未実施。

## 1. 移行原則

原文23件をパッケージの`originals/`にバイト単位で保存し、`review/source_inventory.json`にhashを記録した。`updated_docs/`が改訂版である。原文の実験条件・旧判定を新基準で書き換えない。過去runの「PASS」は当該記録の主張であり、本改訂で独立確認したPASSではない。

「原文L番号」は`originals/`のファイル行を指す。改訂版では追記により行番号が変わる。原文hash、見出し、行範囲を合わせて出所を追跡する。実験成果物パスは文書に記載されているだけで、本パッケージに当該ディレクトリは含まない。

## 2. 文書から確認した修復対象

|ID|対象と原文参照|確認範囲|扱い・完了条件|
|---|---|---|---|
|M01|`03_experiment_plan.md` R031、L287–295／`04_research_results.md` L241–259|同一event・同一経路の逆方向について、双方の0tick費用前平均を負とする必須条件は両立しない|文書論理上の欠陥を記録。旧REJECTは不変。新規仕様はgate satisfiabilityと共有経路の証人テストを必須化|
|M02|計画R046、L410–412|09:15 entryの対象が、09:45–10:14の非zero・可用性に条件付く定義|E_execとE_analysisを分離。全pipelineのprefix監査が済むまで実行可能成績の根拠に使わない。影響件数・金額は未確認|
|M03|計画R049、L440–442／結果R049 `…-05`、L1386–1392|全6anchorの当日有効性を共通Eへ要求。後刻anchorによる前刻取引選別の監査が必要|`…-05`の旧INCONCLUSIVEと訂正履歴は保存。後刻zero変更で前刻注文が不変であることを検査。PnL影響は未確認|
|M04|計画R060–R064の共通E、例L19–25、L33–37、L63–65|主・感度の将来entry/exit足を共通可用性条件に含める|参照U・実行可否・結果可観測性を分離。主欠測は0円化せず、影響監査後に別runでのみ再評価|
|M05|結果R003/R004、L889–944、L989–995／R010 L479–497／R052 L1394–1400|価格の意味、限月／roll／調整、出来高・bar時刻が未解決と記載|必要な供給仕様・正規化来歴を取得・照合。取得不能なら別データ版を検討。未解決をPASSへ変更しない|
|M06|結果R032、L196–206|表示平均516.43と553.68の単純差は−37.25で、記載差−10.41と一致しない|文書上の不一致。正しい差・CIは保存台帳と集計式で確定するまで保留。原文の数値は上書きしない|
|M07|計画R039/R058の差分、結果L1459–1461／R033→R047→R061等|同一仕様でないことは独立したfamilyの証拠ではない|familyを関連付け、全条件・試行・既知情報を登録。過去探索の多重性を補正済みと称さない|
|M08|結果R035 L137–144、R041 L1290–1298、R064 L7–11|主成績と説明群の情報量が単一ラベルに集約されている|旧ラベル不変。新runは主情報量・機構情報量・経済・頑健性を別列で報告|
|M09|計画R063/R064、L25・L37など|「依頼本文の固定規則」に依存する未収録の詳細がある|全条件・seed・nuisance・ゲートの正本を回収しhash化。資料にない閾値を推測して補完しない|
|M10|計画R065、L3–13|旧計画はあるが、今回の結果文書に結果節がない。cross-session保有の扱いが要確認|[R065実行前レビュー](15_r065_execution_review.md)に従い保留。未掲載を実システムでの未実行証明としない|

R032については、上表の「−37.25」は表示済みの丸め値同士の算術である。表の合計と件数から原値を還元する場合は丸めの差があり得る。いずれも保存台帳からCIを再計算した結果ではない。

## 3. 段階と成果物

### R0 — 原記録・仕様の棚卸し

入力は本文、既存コードと設定の読取り専用snapshot、保存済みmanifest。価格の新規読取りを伴うものは別の監査runで許可範囲を固定する。R001～R065を単一行の「最新状態」に潰さず、study/spec/run/decision/evidenceの関連を復元する。

完了成果物: `study_registry.json`、`run_registry.json`、`supersession_map.json`、`prior_information_seen.json`、`missing_artifacts.json`。文書だけでは分からないrunはUNKNOWNとする。結果文書の掲載順や最大run連番だけで有効版を選ばない。

**完了条件:** M01/M06/M09/M10の対象仕様・不一致・未取得証拠が一意に追跡でき、旧結果を消さずに現行利用制限を表示できる。

### R1 — 論理・因果性の共通監査

条件マトリクスを一つの正本とし、side、quantile、event包含・排他、日次軸を生成・検査する。R031型の不可能ANDを静的・合成の両面で拒否する。R046/R049型の未来値変更をloader、QC、U、E_exec、signal、orderまで通したテストで検出する。

完了成果物: `spec_audit.json`、`causality_audit.json`、`condition_resolution.json`、`gate_witnesses.json`、`regression_impact.json`。テスト名と実測結果、code/config hashを結び、文章だけの「PASS」を認めない。

**完了条件:** 発注時点までの一致を検証し、将来fill/exitの変化は許す。既存エンジンを変更した場合の旧実験影響は対象を限定して監査し、不明を影響0としない。

### R2 — データ意味・価格来歴の解決

対象の2021～2025年前半ファイルに対応するbar開始／終了ラベル、OHLC種類、出来高単位・非累積性、未約定分・欠損・訂正方針、実限月・切替・調整方式を証拠表へ整理する。公開取引制度と供給者の加工仕様を分ける。

問い合わせが必要なら未送信文案を作る。連絡・購入・外部ダウンロードは別の依頼と権限に従う。証拠が不足したまま同じ問い合わせ監査を無期限に繰り返さず、必要仕様が明らかな代替データを採用できるか判断する。代替データで同じ年月を再評価しても独立市場標本と呼ばない。

完了成果物: `source_semantics_evidence.json`、`price_lineage_audit.json`、`roll_constituents_evidence.json`、`quality_gate.json`、必要なら`dataset_migration.json`。不明な価格・出来高を推定補完しない。

**完了条件:** 対象用途ごとの品質が根拠付きで決まり、重大な時刻／価格意味の不明が解決する。PASS_LIMITEDのままなら、その制約を継承した限定診断まででOOS候補へ進まない。契約別執行解釈を含めPASS_RESEARCHに到達できるかを別に判断する。

### R3 — 評価基盤・校正・完全な計画

固定予定軸から日次集計を一箇所で生成し、条件別metricsとbootstrapの分母一致を検査する。E_exec、E_analysis、0円、null、未完了建玉を分離する。保存済み台帳の再集計は原因・定義・差分を先に固定し、勝った部分だけを取り出さない。

候補の主目的・主対照・co-primary・必須ストレス・診断を指定し、共有合成価格からの成立可能性と、経済的最小効果への校正を行う。結果を見てから許容DD・件数・CIを決めない。資本など未指定の値はnullを維持し、必要段階に入る前に解決する。

**完了条件:** 実行基盤と受入検査が実測で通り、再開バッチのfamily・予算・停止規則・完全な仕様が固定される。文書だけ更新されてもR3完了ではない。

### R4 — 少数のDevelopment再開と終了レビュー

共通規約の最大3仕様版以内で実行する。R065を優先実行すると自動決定せず、cross-session監査とデータ意味を満たしたかで判断する。旧REJECTを新ルールで救済する一括再計算は行わない。

完了成果物: 全runの段階別判定、family単位の知見、棄却・情報量不足・計測不備の分類、予算消費、次に解決すべき問題。S3に通った候補のみS4へ進み、OOSの開封は別審査で行う。

## 4. 旧研究の扱い

R031の旧REJECTは保存し、論理欠陥を理由に主成績を合格にしない。R032のCIや差の正しい値を本文の丸め値から作り直さない。R046やR049の因果性問題は定義上の利用制限として追記し、金額影響を断定しない。R003の本体、R003-Q001、R004訂正版、R053-Q001/Q002、R062-Q001/Q002を同じ状態にまとめない。

再実行する場合は「修復」「新データ版」「経済仮説変更」を区別し、親run・変更理由・既知PnL・経済経路の変化を明記する。研究費用を抑えるため、台帳の再集計だけで解ける不一致に市場データ全再読込みを要求しない。ただし台帳が誤って生成された場合は、それを正しい入力として流用しない。

## 5. 実装担当への引継ぎ

既存README/AGENTS、Pythonコード、config、schemasは今回のアップロード対象に含まれないため、本パッケージでは書き換えていない。実装担当は実際の構成を読み取り、既存CLIを壊さず、新規仕様用の明示的な段階とmanifest検査を追加する。未実装コマンドを利用可能と宣伝しない。

手順は、原版hash確認 → 文書差分レビュー → 共通audit実装 → 合成と旧回帰 → データ証拠 → 完全な新仕様 → 明示的に依頼されたDevelopment実行、の順とする。コード・市場監査が未完了の間は、各新テストの状態をNOT_RUNとする。

## 6. 進捗指標

進捗はR番号の増加や棄却件数だけで測らない。未解決の重大仕様数、完全なデータ証拠の対象範囲、未来参照を防止した境界、条件配線の共通化率、無効runの再発、完全な凍結仕様数、独立検証へ進める候補数を報告する。値は実測して初めて埋める。
