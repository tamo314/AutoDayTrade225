# TASK-R098-Q001: R075 official-night state-conditioned cash meta-selection

Task ID: `TASK-R098-Q001`  
Study ID: `R098-Q001` / family ID: `cash_meta_selection_night_range_state`  
Run ID: `task-r098-q001-state-conditioned-cash-meta-selection-20260916-02`

This is a one-time Development-only, post-R075/R097 hypothesis (`prior_information_seen=true`). Its decision ceiling is **INVESTIGATE**. R078-A, R079-A, R080-A, R081-A, R084-A, and R086-A are exactly the R097 frozen constituents. Their base-cost daily-Net axes and complete-trade ledgers are immutable inputs, are hash-compared against R097 before they are parsed, and are neither rerun nor repaired. No other constituent may be added or removed.

For a scheduled trade date, the R075 official-night range is the complete official NIGHT high minus low from session start through 05:29 JST. The current range is compared only with the newest 20 calendar-eligible preceding nights within 60 calendar days; the current night is excluded. Nearest-rank q25/q75 gives L=`R<=q25`, M=`q25<R<q75`, H=`R>=q75`. Fewer than 20 references, R004 quarantine, an incomplete official night, or `q25>=q75` makes the state unavailable. This information is available before 08:45 JST. No same-date signal, price, event, fill, exit, or availability is used for a state or a selection.

The first 252 scheduled TSE dates are calibration only. Thereafter every known-state date is evaluable. The matching-state score uses the newest 60 strict-prior scheduled observations drawn from at most the preceding 360 scheduled dates; no-event days remain JPY0. If fewer than 60 exist, A and Q are cash while the date remains in the common evaluation axis. Otherwise A selects the highest mean base-cost daily Net only if it is strictly positive; ties use the frozen ascending strategy ID. Q selects the matching-state second-ranked strategy only if its score is strictly positive. U is the R097 unconditioned 120-scheduled-date control on the same evaluable dates. S uses, for each L/M/H, the highest positive calibration-only state mean, held fixed after calibration. N is cash.

Before performance aggregation, the gate requires matching constituents/hashes and common axes; strict-prior state and score traces; fully classified state-unavailable reasons; at least 650 evaluable dates, 150 in each state, 150 A non-cash selections, 100 completed A trades, two strategies selected at least 20 times, 30 A non-cash selections in every state, 20 completed A trades in each 2022--2024, 10 in 2025H1, and no future reference, same-day availability selection, unexplained exclusion, or unresolved filled position. Failure is INCONCLUSIVE without bootstrap or aggregate performance output.

If the gate passes, all A/U/Q/S estimands use the same 20-trade-date, non-wrapping moving-block bootstrap indices, 10,000 repetitions, seed `20260916`, tail truncation and linear percentiles. Primary AND is A Net>0, PF>1, positive lower CI for A daily Net, A-U, A-Q, and A-S. Any failure is REJECT. Only after a primary pass: reroute with 40 and 80 same-state observations, q20/q80 and q33/q67 states, apply base-A fixed-path two-tick and fee×2 costs (three ticks diagnostic), leave out one family at a time, and remove A's ten largest winning trades. Even a fully passing run remains INVESTIGATE; OOS, Final Holdout and Walk Forward are prohibited.
